import 'package:flutter/material.dart';

class TecnicalPage extends StatelessWidget {

  TecnicalPage({ super.key });

  int index = 1;

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}